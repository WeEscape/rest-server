export interface TaskInterface {
  task_id: string;
  user_id: string;
}

export interface EditTask {
  task_id: string;
  user_id: string;
}

export interface DeleteTask {
  task_id: string;
  user_id: string;
}
