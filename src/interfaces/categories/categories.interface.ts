export interface GetCategory {
  category_id: string;
  user_id: string;
}

export interface EditCategory {
  category_id: string;
  user_id: string;
}

export interface DeleteCategory {
  category_id: string;
  user_id: string;
}
